'use client'; import React from 'react'; interface Region { id: string; name: string; theftCases: number; recoveredAmount: number; status: 'ACTIVE' | 'WARNING' | 'CRITICAL'; percentage: number; } const RegionCards = ({ regions = [] }: { regions: Region[] }) => { const getStatusColor = (status: string) => { switch (status) { case 'CRITICAL': return 'border-l-4 border-red-500 bg-red-50'; case 'WARNING': return 'border-l-4 border-yellow-500 bg-yellow-50'; default: return 'border-l-4 border-green-500 bg-green-50'; } }; const getStatusBadgeColor = (status: string) => { switch (status) { case 'CRITICAL': return 'bg-red-200 text-red-800'; case 'WARNING': return 'bg-yellow-200 text-yellow-800'; default: return 'bg-green-200 text-green-800'; } }; return ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8"> {regions.map((region) => ( <div key={region.id} className={`rounded-lg shadow-lg p-6 ${getStatusColor(region.status)}`}> <div className="flex justify-between items-start mb-4"> <div> <h3 className="text-xl font-bold text-gray-800">{region.name}</h3> <span className={`inline-block mt-2 px-3 py-1 rounded-full text-sm font-semibold ${getStatusBadgeColor(region.status)}`}> {region.status} </span> </div> <div className="text-3xl font-bold text-gray-600">{region.percentage}%</div> </div> <div className="space-y-3"> <div className="flex justify-between items-center"> <span className="text-gray-700 font-medium">Theft Cases:</span> <span className="text-2xl font-bold text-gray-800">{region.theftCases}</span> </div> <div className="flex justify-between items-center"> <span className="text-gray-700 font-medium">Recovered Amount:</span> <span className="text-lg font-bold text-green-600">₹{region.recoveredAmount.toLocaleString()}</span> </div> </div> <div className="mt-4 w-full bg-gray-300 rounded-full h-2"> <div className="bg-blue-600 h-2 rounded-full transition-all duration-300" style={{ width: `${region.percentage}%` }} ></div> </div> </div> ))} </div> ); }; export default RegionCards;